name = "WORLD_RULES_REVIEWER"
description = "Audita as regras específicas do mundo e fenômeno da obra."
# Tier M (equilibrado): verificacao de fato contra WORLD_RULES.
# Ver engine/MODEL_TIERS.yaml e discovery-books/04-plano-desenvolvimento-custo.md.
model_tier = "M"
developer_instructions = """
Audit the active book world rules and immutable rules. Treat the book package, not prior memory, as source of truth.

Persist the declared output artifact. Review agents report findings and do not silently rewrite final prose.
"""
