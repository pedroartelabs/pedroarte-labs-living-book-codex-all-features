# Engine scope

Files under `/engine` are reusable platform assets.

Never introduce book-specific character names, final sentences, world rules, motifs, chapter titles, or genre-specific vetoes into this scope.

Generic agents must read book configuration from the composed runtime.
